/* Fonte e Reset */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Comfortaa', cursive;
    background: linear-gradient(135deg, #FFDEE9, #B5FFFC);
    color: #333;
    overflow-x: hidden;
}

/* Header */
header {
    background: linear-gradient(135deg, #FF6F91, #FF9671);
    padding: 20px 0;
    box-shadow: 0 3px 10px rgba(0,0,0,0.15);
}

nav {
    width: 90%;
    margin: auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

nav h1 { color: white; font-size: 2em; }
nav ul { display: flex; gap: 20px; list-style: none; }
nav ul li a { color: white; text-decoration: none; font-weight: bold; transition: 0.3s; }
nav ul li a:hover { color: #FFD6D6; }

/* Hero */
.hero {
    text-align: center;
    padding: 120px 20px;
    position: relative;
}

.hero h2 { font-size: 2.5em; margin-bottom: 20px; }
.hero p { font-size: 1.2em; margin-bottom: 30px; }

/* Botão pulsante */
.btn {
    padding: 15px 35px;
    border-radius: 50px;
    background: #FF9671;
    color: white;
    font-weight: bold;
    border: none;
    cursor: pointer;
    font-size: 1.1em;
    box-shadow: 0 5px 15px rgba(255,150,113,0.5);
    transition: transform 0.3s, box-shadow 0.3s;
}

.btn:hover {
    transform: scale(1.1);
    box-shadow: 0 8px 20px rgba(255,150,113,0.6);
}

/* Pulsar efeito */
.pulsar {
    animation: pulse 1.5s infinite;
}
@keyframes pulse {
    0% { transform: scale(1); }
    50% { transform: scale(1.15); }
    100% { transform: scale(1); }
}

/* Hearts animados */
.hearts-container {
    position: absolute;
    width: 100%;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    pointer-events: none;
}

.heart {
    font-size: 2em;
    position: absolute;
    animation: float 4s infinite;
    opacity: 0.7;
}

.heart:nth-child(1) { left: 20%; animation-delay: 0s; }
.heart:nth-child(2) { left: 50%; animation-delay: 1s; }
.heart:nth-child(3) { left: 80%; animation-delay: 2s; }

@keyframes float {
    0% { transform: translateY(0) scale(1); opacity: 0.7; }
    50% { transform: translateY(-100px) scale(1.2); opacity: 1; }
    100% { transform: translateY(-200px) scale(1); opacity: 0; }
}

/* Cards Home */
.cards-home {
    display: flex;
    justify-content: center;
    gap: 20px;
    flex-wrap: wrap;
    padding: 60px 20px;
}

.card {
    background: white;
    padding: 30px;
    border-radius: 20px;
    text-align: center;
    width: 260px;
    box-shadow: 0 8px 20px rgba(0,0,0,0.1);
    transition: transform 0.3s, box-shadow 0.3s;
}

.card:hover {
    transform: translateY(-10px);
    box-shadow: 0 15px 25px rgba(0,0,0,0.2);
}

/* Small buttons */
.small-btn {
    display: inline-block;
    margin-top: 15px;
    padding: 10px 20px;
    border-radius: 25px;
    background: #FF8FC7;
    color: white;
    text-decoration: none;
    font-weight: bold;
    transition: 0.3s;
}

.small-btn:hover { background: #FF5EAA; transform: scale(1.05); }

/* Footer */
footer {
    text-align: center;
    padding: 25px;
    background: #FF6F91;
    color: white;
    margin-top: 40px;
}
